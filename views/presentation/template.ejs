# Example
Tutorial

### 目次
- 各種説明
    - 対応デバイス / ブラウザ
    - キーボード操作
    - スライド例
- スライド記法例
    - リスト
    - コード

## 各種説明
詳細は GitHub で

### 対応デバイス / ブラウザ
#### PC
- Google Chrome, Safari, Firefox (アニメーション非対応)

#### スマートフォン
- iPhone / Safari, Google Chrome
- Android / Google Chrome

#### タブレット
- iPad / Safari, Google Chrome
- Android / Google Chrome

### キーボード操作
- 'Enter', 'Space', 'Right', 'j', 'l'  => Next page
- 'Left', 'k', 'h'  => Previous page
- 'o' => ボタンの表示/非表示切り替え
- 'g' (Listener) => Good!
- 'b' (Listener)=> Bad...
- '0' (Listener) => Presenter の現在のページと同期
- '0' (Presenter) => Reset view count (Statistics がおかしくなったら実行する)

## スライド記法例

### リスト
行頭に何もつけないと普通の文章になります

- 行頭に「-」をつけて並べるとリストになります
- 行頭をインデントすると...
    - 入れ子になります
    - インデントはタブでも任意の数のスペースでも大丈夫です
- **強調**もできます

### コード
ソースコードも書けます

```
var me = new Person(&quot;tada&quot;);
for (var day = 1; day <= 14; day++) {
    me.drink(new Redbull());
    me.work();
}
```

# The End
Happy Presentation!
